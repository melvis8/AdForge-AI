databases:
  - name: adforge-db
    plan: free
    databaseName: adforge_db
    user: adforge_user

services:
  - type: web
    name: adforge-api
    runtime: node
    plan: free
    buildCommand: apt-get update -qq && apt-get install -y -qq ffmpeg > /dev/null 2>&1 && cd backend && npm install && npx prisma generate && npx tsc
    startCommand: cd backend && npx prisma db push --skip-generate && node dist/server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: adforge-db
          property: connectionString
      - key: PORT
        value: 3000
      - key: JSON2VIDEO_API_KEY
        sync: false
      - key: ASSEMBLYAI_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: FAL_API_KEY
        sync: false
      - key: B2_APP_KEY_ID
        sync: false
      - key: B2_APP_KEY
        sync: false
      - key: B2_BUCKET_ID
        sync: false
      - key: B2_BUCKET_NAME
        sync: false
